{ ... }:

{
  # Replace this file with the output produced on deeley by:
  #
  #   nixos-generate-config --root /mnt
  #
  # Hardware, filesystems, swap, and initrd modules belong here. GPU-specific
  # configuration can be added later without changing the shared modules.
}

